# TODO: Chatbot UI integration

- [x] Implement `handleUserMessage(message, productData)` and `getLocalResponse(message, productData)` in `frontend/chatbot_ui.js`.
- [ ] Add the chatbot widget DOM (chat icon + chat container) into `frontend/index.html`.
- [ ] Ensure `chatbot_ui.js` is loaded and `window.__chatbot_ui.attachChatHandlers()` is called on DOMContentLoaded.
- [ ] Confirm CSS file `chatbot.css` is loaded on the main page.
- [ ] Manual test: scan product → ask health/ingredients/nutrition → verify UI lock/unlock and fallback.

