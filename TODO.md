# TODO
- [ ] Update plan-approved: recreate modern landing UI (navbar + hero illustration) matching spec.
- [ ] Preserve existing functionality/IDs for scanner/results/history.
- [ ] Update `frontend/index.html` hero section markup (heading, subheading, Scan Barcode + Manual Entry buttons, illustration, glassmorphism card).
- [ ] Update `frontend/style.css` to match new tokens/colors, button styles, illustration floating animation, gradient background animation, and fade-in.
- [ ] Update `frontend/app.js` / (or add small code in `frontend/index.html`) for manual entry button to focus input + smooth scroll.
- [ ] Ensure responsive stacking for small screens.
- [ ] Quick manual test: open `frontend/index.html` in browser; verify nav smooth scroll + button hover animations + scanner still works.
